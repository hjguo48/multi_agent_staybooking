{
  "name": "week6_iterative_feedback_pipeline",
  "description": "Week 6: Iterative Feedback topology with iteration cap and anti-loop controls.",
  "report_path": "outputs/week6/week6_iterative_feedback_pipeline_report.json",
  "steps": [
    {
      "name": "verify_baseline_lock",
      "command": [
        "${PYTHON}",
        "evaluation/verify_baseline_lock.py"
      ]
    },
    {
      "name": "validate_prompt_contracts",
      "command": [
        "${PYTHON}",
        "evaluation/validate_prompt_contracts.py"
      ]
    },
    {
      "name": "week6_iterative_feedback_smoke",
      "command": [
        "${PYTHON}",
        "evaluation/week6_iterative_feedback_smoke.py"
      ]
    },
    {
      "name": "unit_tests",
      "command": [
        "${PYTHON}",
        "-m",
        "unittest",
        "discover",
        "-s",
        "tests",
        "-p",
        "test_*.py"
      ]
    }
  ]
}
